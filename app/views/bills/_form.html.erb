<%= f.text_field :name %>
<br/>
<%= f.text_field :total_amount, append: "RON"  %>
<br/>
<%= f.text_area :description %>
<br/>
<%= f.label :participants %>
<br/>
<%= f.select(:participant_ids, options_from_collection_for_select(current_user_participants, :id, :name_with_initial, f.object.participant_ids), {}, { :multiple => true}) %>
<br/>
<%= f.submit "Save" %>
<%= link_to "Cancel", bills_path, class: 'btn btn-primary', style:"width:80px" %>
